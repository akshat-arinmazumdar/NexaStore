const nextAuth = require('next-auth');
console.log("Exports of next-auth:", Object.keys(nextAuth));
const nextAuthNext = require('next-auth/next');
console.log("Exports of next-auth/next:", Object.keys(nextAuthNext));
